version: '3.8'

services:
  quanto_vale_be_redis: # Needs updating
    image: redis:alpine
    container_name: quanto_vale_be_redis # Needs updating
    ports:
      - '6379:6379'
    volumes:
      - quanto_vale_be_redis_data:/data # Needs updating

networks:
  default:
    name: quanto_vale_be # Needs updating

volumes:
  quanto_vale_be_redis_data: # Needs updating